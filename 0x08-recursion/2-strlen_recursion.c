#include <stdio.h>
#include <string.h>
#include "main.h"

/**
  * _strlen_recursion - the length of a string
  * @s: a string
  *
  * Return: Length
  */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
