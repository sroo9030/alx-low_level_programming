#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
  * _atoi - convert a string to an integer
  * @s: a char type parameter
  *
  * Return: an integer
  */
int _atoi(char *s)
{
	int i;

	i = atoi(s);
	return (i);
}
